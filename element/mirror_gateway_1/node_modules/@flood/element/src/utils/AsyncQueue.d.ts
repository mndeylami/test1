export declare class AsyncQueue<Specifier> {
    chain: Promise<Specifier | null>;
    constructor(chain?: Promise<Specifier | null>);
    add(task: PromiseLike<Specifier>): Promise<Specifier>;
    add(task: (value: any) => Specifier): Promise<Specifier>;
}
