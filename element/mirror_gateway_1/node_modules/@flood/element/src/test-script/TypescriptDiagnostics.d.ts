import { Diagnostic } from 'typescript';
import * as ts from 'typescript';
declare type diagnosticCategory = 'warnings' | 'errors' | 'messages';
declare type FilenameMapper = (from: string) => string;
export declare class CategorisedDiagnostics {
    private formatHost;
    private filenameMapper;
    diagnostics: {
        [DKey in diagnosticCategory]: Diagnostic[];
    };
    constructor(formatHost: ts.FormatDiagnosticsHost, filenameMapper: FilenameMapper);
    has(kind: diagnosticCategory): boolean;
    hasNonErrors(): boolean;
    hasOnlyNonErrors(): boolean;
    add(d: Diagnostic): void;
    forCategory(kind: diagnosticCategory): Diagnostic[];
    formattedForCategory(kind: diagnosticCategory): string;
    stringForCategory(kind: diagnosticCategory, delim?: string): string;
    format(kind: diagnosticCategory): string[];
    formatDiagnostic(diagnostic: Diagnostic): string;
}
export {};
