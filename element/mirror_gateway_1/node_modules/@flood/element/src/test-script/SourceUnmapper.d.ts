import { SourceMapConsumer } from 'source-map';
export interface Callsite {
    file: string;
    code: string;
    line: number;
    column: number;
}
interface StackLine {
    at: string;
    file: string;
    line: number;
    column: number;
}
export declare function callsiteToString(callsite: Callsite | undefined): string;
export declare class SourceUnmapper {
    private originalSource;
    private originalFilename;
    private sourceMapConsumer;
    static init(originalSource: string, originalFilename: string, sourceMap: string): Promise<SourceUnmapper>;
    constructor(originalSource: string, originalFilename: string, sourceMapConsumer: SourceMapConsumer);
    unmapNodeStackLine(stackLine: string): string;
    unmapCallsite(stackLine: string): Callsite | undefined;
    unmapStack(stack: string[]): StackLine[];
    parseStack(stack: string[]): StackLine[];
    originalPositionFor(pos: StackLine): StackLine | undefined;
    unmapStackNodeStrings(stack: string[]): string[];
    extractSourceLine(line: number): string;
    stackLineToNodeString(m: StackLine): string;
}
export {};
