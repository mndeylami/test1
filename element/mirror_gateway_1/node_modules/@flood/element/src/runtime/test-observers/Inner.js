"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Observer_1 = require("./Observer");
const Settings_1 = require("../Settings");
class InnerObserver extends Observer_1.NoOpTestObserver {
    constructor(next) {
        super(next);
    }
    async beforeStepAction(test, step, command) {
        if (test.settings.actionDelay > 0 && command !== 'wait') {
            await new Promise(resolve => {
                // TODO fix default
                setTimeout(resolve, test.settings.actionDelay * 1e3 || Settings_1.DEFAULT_ACTION_WAIT_SECONDS * 1e3);
            });
        }
        return this.next.beforeStepAction(test, step, command);
    }
}
exports.default = InnerObserver;
