"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.emptyErrorData = { _kind: 'empty' };
function interpretError(interpreters, inputError, target, propertyKey, args) {
    for (const interp of interpreters) {
        const outputErr = interp(inputError, target, propertyKey, ...args);
        if (outputErr !== undefined)
            return outputErr;
    }
    return inputError;
}
exports.interpretError = interpretError;
function castStructuredError(error, kind) {
    if (error._structured === 'yes' &&
        error.data._kind === kind) {
        return error;
    }
    else {
        return undefined;
    }
}
exports.castStructuredError = castStructuredError;
function getStructuredData(error, kind) {
    if (error._structured === 'yes' &&
        error.data._kind === kind) {
        return error.data;
    }
}
exports.getStructuredData = getStructuredData;
