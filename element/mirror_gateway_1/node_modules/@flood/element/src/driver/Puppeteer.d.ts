import { LaunchOptions, Browser, Page } from 'puppeteer';
import { ChromeVersion } from '../runtime/Settings';
export declare type ConcreteLaunchOptions = LaunchOptions & {
    args: string[];
    chromeVersion: ChromeVersion | string;
    sandbox: boolean;
};
export interface IPuppeteerClient {
    browser: Browser;
    page: Page;
    close(): Promise<void>;
    reopenPage(): Promise<void>;
}
export declare class PuppeteerClient implements IPuppeteerClient {
    browser: Browser;
    page: Page;
    constructor(browser: Browser, page: Page);
    private _isClosed;
    close(): Promise<void>;
    reopenPage(): Promise<void>;
}
export declare function launch(passedOptions?: Partial<ConcreteLaunchOptions>): Promise<PuppeteerClient>;
export declare class NullPuppeteerClient implements IPuppeteerClient {
    browser: Browser;
    page: Page;
    constructor();
    close(): Promise<void>;
    reopenPage(): Promise<void>;
}
