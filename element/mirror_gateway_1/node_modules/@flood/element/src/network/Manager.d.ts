import { Page } from 'puppeteer';
export declare class Manager {
    private page;
    private requestIdToRequest;
    private lifecycleCompleteCallback;
    private networkIdlePromise;
    private maximumTimer;
    timeout: number;
    constructor(page: Page, requestIdToRequest?: Map<string, any>);
    getIdlePromise(): Promise<void>;
    readonly pendingRequestCount: number;
    private updateIdlePromise;
    private createTimeoutPromise;
    private createIdlePromise;
    private attachEvents;
    private onRequestWillBeSent;
    private handleRequestStart;
    private onRequestIntercepted;
    private onResponseReceived;
    private onLoadingFinished;
    private onLoadingFailed;
}
