import { Condition } from '../Condition';
import { Frame } from 'puppeteer';
export declare class URLCondition extends Condition {
    url: string;
    partial: boolean;
    constructor(desc: string, url: string, partial?: boolean);
    toString(): string;
    waitFor(frame: Frame): Promise<boolean>;
}
