import { Condition, NullableLocatable } from '../Condition';
import { Frame, Page } from 'puppeteer';
export declare class FrameCondition extends Condition {
    id: NullableLocatable;
    constructor(desc: string, id: NullableLocatable);
    toString(): string;
    waitFor(frame: Frame, page: Page): Promise<Frame | Error>;
    private maximumTimer;
    private createTimeoutPromise;
}
