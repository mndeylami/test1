import { Condition } from '../Condition';
import { Frame } from 'puppeteer';
export declare class TitleCondition extends Condition {
    expectedTitle: string;
    partial: boolean;
    constructor(desc: string, expectedTitle: string, partial?: boolean);
    toString(): string;
    waitFor(frame: Frame): Promise<boolean>;
}
