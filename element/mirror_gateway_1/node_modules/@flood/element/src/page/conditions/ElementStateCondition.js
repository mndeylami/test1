"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Condition_1 = require("../Condition");
class ElementStateCondition extends Condition_1.ElementCondition {
    constructor(desc, locator, ...args) {
        super(desc, locator);
        this.pageFunc = (node, waitForDisabled) => {
            if (!node)
                return false;
            return node['disabled'] === waitForDisabled;
        };
        this.pageFuncArgs = args;
    }
    toString() {
        let [disabled] = this.pageFuncArgs;
        return `for element to become ${disabled ? 'disabled' : 'enabled'}`;
    }
}
exports.ElementStateCondition = ElementStateCondition;
