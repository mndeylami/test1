import { ElementCondition, NullableLocatable } from '../Condition';
import { EvaluateFn } from 'puppeteer';
export declare class ElementVisibilityCondition extends ElementCondition {
    constructor(desc: string, locator: NullableLocatable, ...args: any[]);
    toString(): string;
    pageFunc: EvaluateFn;
}
export declare class ElementLocatedCondition extends ElementCondition {
    constructor(desc: string, locator: NullableLocatable, ...args: any[]);
    toString(): string;
    pageFunc: EvaluateFn;
}
export declare class ElementsLocatedCondition extends ElementCondition {
    desc: string;
    constructor(desc: string, locator: NullableLocatable, ...args: any[]);
    toString(): string;
    readonly locatorPageFunc: EvaluateFn;
    pageFunc: EvaluateFn;
}
