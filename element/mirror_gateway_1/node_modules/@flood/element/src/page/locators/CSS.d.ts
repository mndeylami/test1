import { BaseLocator } from '../Locator';
import { EvaluateFn } from 'puppeteer';
export declare class CSSLocator extends BaseLocator {
    selector: string;
    constructor(selector: string, debugString: string);
    readonly pageFuncArgs: string[];
    readonly pageFunc: EvaluateFn;
    readonly pageFuncMany: EvaluateFn;
    toString(): string;
}
