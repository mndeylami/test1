import { BaseLocator } from '../Locator';
import { EvaluateFn } from 'puppeteer';
export declare class LinkTextLocator extends BaseLocator {
    linkText: string;
    partial: boolean;
    constructor(linkText: string, partial: boolean, desc: string);
    readonly pageFuncArgs: any[];
    readonly pageFunc: EvaluateFn;
    readonly pageFuncMany: EvaluateFn;
    toString(): string;
}
