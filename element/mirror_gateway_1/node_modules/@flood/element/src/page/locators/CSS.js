"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Locator_1 = require("../Locator");
class CSSLocator extends Locator_1.BaseLocator {
    constructor(selector, debugString) {
        super(debugString);
        this.selector = selector;
    }
    get pageFuncArgs() {
        return [this.selector];
    }
    get pageFunc() {
        return (selector, node) => (node ? node : document).querySelector(selector);
    }
    get pageFuncMany() {
        return (selector, node) => Array.from((node ? node : document).querySelectorAll(selector));
    }
    toString() {
        return `css(${this.selector})`;
    }
}
exports.CSSLocator = CSSLocator;
